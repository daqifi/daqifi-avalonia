// Generated by portomatic scaffold from upstream Daqifi.Desktop/Device/Firmware/BootloaderSessionStreamingDeviceAdapter.cs.
//
// DO NOT manually delete the `// @port:` markers — they link symbols back to
// the correspondence map.

using System.Net;
using Daqifi.Core.Channel;
using Daqifi.Core.Communication.Messages;
using CoreConnectionStatus = Daqifi.Core.Device.ConnectionStatus;
using CoreDeviceStatusEventArgs = Daqifi.Core.Device.DeviceStatusEventArgs;
using CoreMessageReceivedEventArgs = Daqifi.Core.Device.MessageReceivedEventArgs;
using CoreStreamingDevice = Daqifi.Core.Device.IStreamingDevice;

namespace Daqifi.Desktop.Device.Firmware;

/// <summary>
/// Minimal adapter for manual bootloader-only update dialogs.
/// The device is already in firmware mode, so serial operations are intentionally no-op.
/// </summary>
// @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter
public sealed class BootloaderSessionStreamingDeviceAdapter : CoreStreamingDevice
{
    private bool _isConnected = true;

    public BootloaderSessionStreamingDeviceAdapter(string name)
    {
        Name = string.IsNullOrWhiteSpace(name) ? "DAQiFi Bootloader" : name;
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.Name
    public string Name { get; }
    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.IpAddress
    public IPAddress? IpAddress => null;
    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.IsConnected
    public bool IsConnected => _isConnected;
    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.Status
    public CoreConnectionStatus Status => _isConnected
        ? CoreConnectionStatus.Connected
        : CoreConnectionStatus.Disconnected;

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.StatusChanged
    public event EventHandler<CoreDeviceStatusEventArgs>? StatusChanged;
    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.MessageReceived
    public event EventHandler<CoreMessageReceivedEventArgs>? MessageReceived;

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.StreamingFrequency
    public int StreamingFrequency { get; set; } = 1;
    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.IsStreaming
    public bool IsStreaming => false;

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.StartStreaming
    public void StartStreaming()
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.StopStreaming
    public void StopStreaming()
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.Connect
    public void Connect()
    {
        if (_isConnected)
        {
            return;
        }

        _isConnected = true;
        StatusChanged?.Invoke(this, new CoreDeviceStatusEventArgs(CoreConnectionStatus.Connected));
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.Disconnect
    public void Disconnect()
    {
        if (!_isConnected)
        {
            return;
        }

        _isConnected = false;
        StatusChanged?.Invoke(this, new CoreDeviceStatusEventArgs(CoreConnectionStatus.Disconnected));
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.Send<T>
    public void Send<T>(IOutboundMessage<T> message)
    {
        // No-op by design: bootloader-only flow does not use desktop serial command transport.
    }

    // Channel management and device-control members (channel/DIO/analog-output members added to
    // Core's IStreamingDevice in 0.24.0; the PWM members and PwmFrequencyHz in 1.0.0; the ADC
    // calibration and NVM persistence members in 1.3.0) are intentionally no-op here: the device
    // is already in firmware-update mode and this adapter does not configure channels, drive
    // outputs, calibrate, persist NVM, or reboot through the streaming command path.
    // PwmFrequencyHz => 0 matches Core's documented "none commanded this session" sentinel.
    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.EnableChannel
    public void EnableChannel(IChannel channel)
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.EnableChannels
    public void EnableChannels(IEnumerable<IChannel> channels)
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.DisableChannel
    public void DisableChannel(IChannel channel)
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.DisableAllChannels
    public void DisableAllChannels()
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.SetDioDirection
    public void SetDioDirection(IChannel channel, ChannelDirection direction)
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.SetDioValue
    public void SetDioValue(IChannel channel, bool value)
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.SetPwmEnabled
    public void SetPwmEnabled(IChannel channel, bool enabled)
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.SetPwmDutyCycle
    public void SetPwmDutyCycle(IChannel channel, int dutyCyclePercent)
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.SetPwmFrequency
    public void SetPwmFrequency(int frequencyHz)
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.PwmFrequencyHz
    public int PwmFrequencyHz => 0;

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.SetAnalogOutput
    public void SetAnalogOutput(int channelNumber, double voltage)
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.Reboot
    public void Reboot()
    {
    }

    // ADC calibration + voltage-precision members (added to Core's IStreamingDevice in 1.3.0) are
    // intentionally no-op here for the same reason as above: a bootloader-only adapter never
    // calibrates the ADC or persists calibration/precision to device NVM.
    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.SaveAdcCalibration
    public void SaveAdcCalibration()
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.LoadAdcCalibration
    public void LoadAdcCalibration()
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.SetAdcCalibrationSlope
    public void SetAdcCalibrationSlope(int channelNumber, double calM)
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.SetAdcCalibrationOffset
    public void SetAdcCalibrationOffset(int channelNumber, double calB)
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.SaveFactoryAdcCalibration
    public void SaveFactoryAdcCalibration()
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.LoadFactoryAdcCalibration
    public void LoadFactoryAdcCalibration()
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.UseAdcCalibration
    public void UseAdcCalibration(int bank)
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.SaveVoltagePrecision
    public void SaveVoltagePrecision()
    {
    }

    // @port: Daqifi.Desktop.Device.Firmware.BootloaderSessionStreamingDeviceAdapter.LoadVoltagePrecision
    public void LoadVoltagePrecision()
    {
    }
}
