// Generated by portomatic scaffold from upstream Daqifi.Desktop/Helpers/EnumDescriptionConverter.cs.
//
// DO NOT manually delete the `// @port:` markers — they link symbols back to
// the correspondence map.

using System.ComponentModel;
using System.Globalization;
using Avalonia.Data.Converters;

namespace Daqifi.Desktop.Helpers;

// @port: Daqifi.Desktop.Helpers.EnumDescriptionConverter
public class EnumDescriptionConverter : IValueConverter
{
    // @port: Daqifi.Desktop.Helpers.EnumDescriptionConverter.GetEnumDescription
    private string GetEnumDescription(Enum enumObj)
    {
        var fieldInfo = enumObj.GetType().GetField(enumObj.ToString());

        var attribArray = fieldInfo.GetCustomAttributes(false);

        if (attribArray.Length == 0)
        {
            return enumObj.ToString();
        }

        var attrib = attribArray[0] as DescriptionAttribute;
        return attrib.Description;

    }

    // @port: Daqifi.Desktop.Helpers.EnumDescriptionConverter.Convert
    object IValueConverter.Convert(object value, Type targetType, object parameter, CultureInfo culture)
    {
        if (value == null)
        {
            return string.Empty;
        }

        if (value is not Enum myEnum)
        {
            return value.ToString();
        }

        var description = GetEnumDescription(myEnum);
        return description;
    }

    // @port: Daqifi.Desktop.Helpers.EnumDescriptionConverter.ConvertBack
    object IValueConverter.ConvertBack(object value, Type targetType, object parameter, CultureInfo culture)
    {
        return string.Empty;
    }
}
